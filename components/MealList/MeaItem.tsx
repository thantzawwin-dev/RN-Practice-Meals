import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const MeaItem = (props: Props) => {
  return (
    <View>
      <Text>MeaItem</Text>
    </View>
  );
};

export default MeaItem;

const styles = StyleSheet.create({});
